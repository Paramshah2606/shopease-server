<!DOCTYPE html>
<html>
<head>
  <title>Password Reset</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="icon" type="image/x-icon" href="/utilities/iconedsoma.png">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css">
  <style>
    body {
      background: linear-gradient(135deg, #e0e7ff 0%, #f8fafc 100%);
      min-height: 100vh;
      font-family: 'Segoe UI', Arial, sans-serif;
    }
    .login_form {
      background: #fff;
      padding: 2.5rem 2rem 2rem 2rem;
      border-radius: 18px;
      box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.18);
      margin-top: 60px;
      animation: fadeIn 0.7s;
    }
    .illustration {
      display: block;
      margin: 0 auto 18px auto;
      width: 90px;
      height: 90px;
    }
    .form-header {
      text-align: center;
      font-size: 1.5rem;
      font-weight: 600;
      color: #374151;
      margin-bottom: 8px;
    }
    .form-desc {
      text-align: center;
      color: #64748b;
      font-size: 1rem;
      margin-bottom: 24px;
    }
    .errmsg {
      background: #fee2e2;
      color: #b91c1c;
      border: 1px solid #fecaca;
      padding: 0.7rem 1rem;
      border-radius: 6px;
      margin-bottom: 12px;
      text-align: center;
      font-size: 1rem;
      animation: shake 0.3s;
    }
    .successmsg {
      background: #dcfce7;
      color: #166534;
      border: 1px solid #bbf7d0;
      padding: 1rem 1.2rem;
      border-radius: 6px;
      margin-bottom: 18px;
      text-align: center;
      font-size: 1.1rem;
      animation: fadeIn 0.7s;
    }
    .form-group label {
      font-weight: 500;
      color: #374151;
      margin-bottom: 6px;
    }
    .form-control {
      border-radius: 8px;
      border: 1px solid #cbd5e1;
      transition: border-color 0.2s;
    }
    .form-control:focus {
      border-color: #6366f1;
      box-shadow: 0 0 0 2px #6366f122;
    }
    .form_btn {
      width: 100%;
      padding: 0.7rem 0;
      font-size: 1.1rem;
      border-radius: 8px;
      background: linear-gradient(90deg, #6366f1 60%, #818cf8 100%);
      border: none;
      transition: background 0.2s, transform 0.1s;
      box-shadow: 0 2px 8px rgba(99,102,241,0.07);
    }
    .form_btn:hover {
      background: linear-gradient(90deg, #818cf8 60%, #6366f1 100%);
      transform: translateY(-2px) scale(1.02);
    }
    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(30px);}
      to { opacity: 1; transform: translateY(0);}
    }
    @keyframes shake {
      0% { transform: translateX(-5px);}
      25% { transform: translateX(5px);}
      50% { transform: translateX(-5px);}
      75% { transform: translateX(5px);}
      100% { transform: translateX(0);}
    }
    @media (max-width: 575px) {
      .login_form {
        padding: 1.2rem 0.7rem;
        margin-top: 30px;
      }
      .illustration {
        width: 60px;
        height: 60px;
      }
    }
  </style>
</head>
<body>
<div class="container">
  <div class="row justify-content-center">
    <div class="col-sm-8 col-md-6 col-lg-4">
      <div class="login_form">
        <% if (hide && error) { %>
          <!-- Expired/Invalid Link Section -->
          <svg class="illustration" viewBox="0 0 64 64" fill="none">
            <rect width="64" height="64" rx="16" fill="#fee2e2"/>
            <path d="M32 18a10 10 0 0 1 10 10v4h-2v-4a8 8 0 0 0-16 0v4h-2v-4a10 10 0 0 1 10-10zm-8 14v10a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V32a2 2 0 0 0-2-2H26a2 2 0 0 0-2 2zm2 0h12v10H26V32z" fill="#b91c1c"/>
          </svg>
          <div class="form-header">Link Expired</div>
          <div class="form-desc">
            Sorry, this password reset link is no longer valid.<br>
            Please request a new password reset if you still need access.
          </div>
        <% } else { %>
          <!-- Normal Password Reset Section -->
          <svg class="illustration" viewBox="0 0 64 64" fill="none">
            <rect width="64" height="64" rx="16" fill="#6366f1"/>
            <path d="M32 18a10 10 0 0 1 10 10v4h-2v-4a8 8 0 0 0-16 0v4h-2v-4a10 10 0 0 1 10-10zm-8 14v10a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V32a2 2 0 0 0-2-2H26a2 2 0 0 0-2 2zm2 0h12v10H26V32z" fill="#fff"/>
          </svg>
          <div class="form-header">Reset Your Password</div>
          <div class="form-desc">
            Please enter your new password below.<br>
            Make sure it’s strong and unique to keep your account secure.
          </div>

          <form action="/reset-password" method="POST" autocomplete="off">
            <% if (error) { error.forEach(e => { %>
              <div class="errmsg"><%= e %></div>
            <% }) } %>

            <% if (success) { %>
              <div class="successmsg">
                <%= success %>
              </div>
            <% } %>

            <% if (!hide) { %>
              <input type="hidden" name="token" value="<%= token %>">
              <div class="form-group">
                <label>Password</label>
                <input type="password" name="password" class="form-control" required placeholder="Enter new password">
              </div>
              <div class="form-group">
                <label>Confirm Password</label>
                <input type="password" name="passwordConfirm" class="form-control" required placeholder="Re-enter new password">
              </div>
              <button type="submit" name="sub_set" class="btn btn-primary btn-group-lg form_btn">Reset Password</button>
            <% } %>
          </form>
        <% } %>
      </div>
    </div>
  </div>
</div>
<script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
